export interface Shop {
  category: string;
  domain: string;
  location: Array<number>;
  name: string;
  parent: string;
  sonectId: string;
  distance: number;
  status: number;
  workers: Array<any>;
}
